[project]
name = "lawfirm-mcpserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "youssefelgamel", email = "youssefelgamel12@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.1",
    "mcp[cli]>=2.0.0",
    "pydantic>=2.13.4",
]

[project.scripts]
lawfirm-mcpserver = "lawfirm_mcpserver:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
